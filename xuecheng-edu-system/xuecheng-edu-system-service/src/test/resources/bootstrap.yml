server:
  servlet:
    context-path: /system
  port: 63110

#微服务配置
spring:
  profiles:
    active: dev-local # use dev config for local PC
  application:
    name: system-service-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml

# swagger 文档配置
#swagger:
#  title: "学成在线系统管理"
#  description: "系统管理接口"
#  base-package: com.xuecheng.system
#  enabled: true
#  version: 1.0.0

--- # specific config for local PC develop environment
spring:
  profiles: dev-local
  datasource:
    url: jdbc:mysql://localhost:3305/xc_edu_system?serverTimezone=UTC&userUnicode=true&useSSL=false
    username: root
    password: root

--- # specific config for local develop environment (in local virtual machine CentOS)
spring:
  profiles: dev-local-vm
  datasource:
    url: jdbc:mysql://192.168.101.65:3306/xc_edu_system?serverTimezone=UTC&userUnicode=true&useSSL=false
    username: root
    password: mysql